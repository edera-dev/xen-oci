[Unit]
Description=OCaml-based Xenstore

[Service]
Restart=on-failure
Type=forking
PreStart=/bin/mkdir -p /var/log/xen
PIDFile=/var/run/xen/xenstored.pid
ExecStart=/usr/sbin/oxenstored
User=root

[Install]
WantedBy=multi-user.target
